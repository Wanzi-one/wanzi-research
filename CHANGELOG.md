# 更新记录

## v1.0.2 — 2026-09-15

### 报告格式收紧

- 第七步「分析与交付」明确：**最终报告必须输出为 HTML 文件**（基于 `assets/report-template.html` 奶油黄骨架），Markdown 只能作为附加摘要，不能替代；仅用户明确要求时才降级为 md。
- 交付验收清单新增对应检查项。
- 背景：部分使用者反馈最终报告为 md 格式——原「报告模板见 assets/...」为软约束，不同 AI 执行时会自由发挥，现改为硬性纪律。

## v1.0.1 — 2026-09-11

### 许可变更（重要）

- 许可由 MIT 改为**自定义许可：免费使用 · 保留署名 · 禁止转卖**（见 `LICENSE`）。
  使用者仍可免费使用（含商业机构内部使用）、修改与分发衍生作品，但**不得**出售、
  出租、按订阅收费，或整体打包进付费课程／社群／工具包；需要对外提供付费服务或
  商业嵌入，请联系作者取得单独授权。
- `LICENSE` 版权署名更新为「自由碗子（Wanzi-one）」。
- 同步更新 `README.md`（目录说明、使用须知、许可章节）与 `references/configuration.md` 的许可表述。

> 若你在此版本之前已取得 v1.0.0（MIT），该副本仍按 MIT 条款授权；v1.0.1 起适用新许可。

## v1.0.0 — 2026-09-11

首次公开发布。

### 采集内核（scripts/）

- `api_request.py`：密钥安全读取与脱敏预览、请求数与费用预估、响应落盘、请求记账（ledger）、失败退避重试；支持 `--official-price` 向数据网关核准单价、`--dry-run` 空跑预览且不消耗请求。
- `collect_notes.py`：账号作品全量翻页采集，按内容 ID 去重、置顶项在跨页重复时自动折抵，`--resume` 断点续跑；连续失败两次自动停止并保留已完成分页，可续跑。
- `search_pick.py`：关键词搜索 + 低粉高数据筛选，自动补齐作者真实粉丝并按效率分排序。所有评分公式均标注为经验权重，不冒充平台官方指标。
- `normalize.py`：统一字段表、跨页去重、数据摘要与时间线生成；日期解析标注置信度（`exact` / `inferred_year` / `relative` / `unknown`），避免把不完整日期写成年度结论。

### 方法论（references/）

- `analysis-framework.md`：四维拆解框架（人设 / 人群 / 内容 / 商业）、三层人设结构、处境式人群推导、主线支线切分、六类变现路径、评论区三类信号、证据五级标注、报告九段结构。
- `api-endpoints.md`：实测端点参数与坑位记录（含评论区排序策略降级、搜索结果作者字段差异、日期字段缺失等）。
- `output-schema.md`：统一输出字段表与日期置信度口径。
- `configuration.md`：密钥配置方式与 macOS 非交互 shell 读不到环境变量的排查步骤。

### 报告模板

- `assets/report-template.html`：可直接套用的拆解报告版式，含数据卡、时间线、对比表与引用块。

### 执行纪律（P0，任何情况下不绕过）

1. 批量采集前必须先说明计价方式、给出请求数与费用预览，且只跑 1–3 条样本；
2. 密钥不进入对话、不写入技能文件、不落进报告；
3. 结论必须能回指原始响应文件，无法确证处标注「待验证」。
