---
name: wanzi-research
description: >
  碗子调研：用你自己配置的付费数据网关（默认 TikHub）做全平台社媒调研与对标拆解，
  采集公开的账号资料、作品、评论、字幕、搜索与话题数据，并输出可回指原始证据的
  JSON / CSV / Excel 与四维拆解报告。当用户提到 碗子调研、社媒调研、全平台调研、
  博主调研、账号拆解、对标账号、对标拆解、抓作品、抓评论、抓逐字稿、关键词选题、
  低粉高数据、找爆款、四维拆解、人设定位、人群定位、内容定位、商业定位、
  TikHub、抖音、小红书、视频号、TikTok、YouTube、B站、快手、微博、Instagram、
  X、知乎、Reddit，或要求监控社媒公开数据时使用。执行任何可能产生费用的批量采集前，
  必须先说明计价方式、给出请求数与费用预览，并只跑 1–3 条样本。
---

# 碗子调研

把「帮我看看这个博主」变成一条可复现的流水线：**查端点 → 算费用 → 跑样本 → 批量采集 → 归档原始证据 → 四维拆解成结论**。

调研的价值不在抓到数据，而在于结论能被原始文件回指核实。所以本技能所有环节都遵守一条主线：**原始响应永不加工，推断与事实分开存放。**

## 四条执行纪律（P0，任何情况下不绕过）

### 1. 先把钱说清楚

采集走用户自己配置的付费数据网关，费用由用户承担。任何可能扣费的批量请求前，先给出这张预览：

```markdown
## 付费请求预览
- 调研对象：
- 使用端点：
- 请求拆分：账号资料 __ 次 + 作品列表 __ 次 + 详情 __ 次 + 评论 __ 次
- 预计总请求：__ 次
- 端点单价：__ USD / 次（来源与查询时间：__）
- 预计费用：__ USD
- 不含：第三方转写、特殊高价端点、失败重试与临时扩量
- 执行方式：先跑 1–3 条样本，字段确认后再批量
```

只写「预估」，不承诺固定金额。判断口径和最新价格以网关官方页面与价格计算接口为准，不拿历史数字当报价。

### 2. 先小样本，再批量

样本通过标准：对象正确、核心字段存在、分页含义明确、无权限或余额错误、成本与预估同量级。

样本不通过 → **停在这里**修端点或缩范围，不要靠重试硬碰。

### 3. 密钥永不进聊天

- Key 只从本机环境变量或系统钥匙串读取；
- 不要求用户把 Key 粘贴到对话里；
- 不把 Key 写进脚本、报告、截图或 Git；
- 输出与日志里出现 `Authorization`、`token=`、`sign=`、`cache_url` 等可复用凭据时必须脱敏。

### 4. 推断必须标注

- 报告里每一个数字，都要能回指某个原始文件或链接；
- 平台没给的数字（如某些端点的播放量恒为 0）不要当成结论，直接说明「该字段不可用」；
- 由分析推出的判断（如客单价、销量、转化率）必须标注 **待验证**，并写明推断依据；
- 不把品牌自述话术（如「全网第一」「复购七成」）写成客观事实，需注明来源是对方自述。

## 能力边界

**本技能可以：**

- 采集用户有权访问的公开社媒数据：账号资料、作品列表、单条详情、评论、字幕、搜索结果；
- 处理用户已有的 Excel / CSV / JSON / 截图清单；
- 输出账号表、作品表、评论表、逐字稿、证据索引；
- 按四维框架产出对标拆解报告与选题方向。

**本技能不：**

- 自带任何 Key、免费数据源、Cookie 或平台登录态；
- 代理、转售或背书任何数据服务商，也不承诺其价格与稳定性；
- 绕过登录、验证码、付费墙或平台访问限制；
- 抓取创作者后台的留存、流量来源等非公开数据；
- 用本地语音模型（Whisper 等）临时转写充数——逐字稿只认平台官方字幕或用户自行配置的第三方 ASR。

## 路由边界

**用本技能：** 全平台调研、博主/账号拆解、对标分析、关键词选题筛选、抓评论找需求、拉历史作品、导出社媒数据。

**不用本技能：**

- 公众号文章正文导出 → 优先用用户现有的导出工具；
- 本机微信聊天/朋友圈本地数据；
- 普通网页与博客；
- 只写口播稿、朋友圈文案等纯创作任务（那属于内容创作技能）。

## 与相邻技能的配合

本技能负责**账号级**工作：给一个账号 → 采集全量数据 → 四维定位拆解。

如果当前环境里还有**内容级**的创作机制拆解技能（逐条拆解标题、钩子、结构、情绪设计等维度），两者是上下游，正确串联顺序是：

```text
本技能拆账号 → 挑出 2–3 条代表作 → 交给内容级技能拆机制 → 回到本技能产出选题
```

不要用本技能重复做单篇内容的机制拆解，也不要在内容级技能里重复采集账号全量数据。

## 平台路由

| 平台 | 首选路线 |
| --- | --- |
| 抖音 | 网关的 Douyin Web / App / Search 端点；搜索用 `fetch_general_search_v3`，账号作品用 `fetch_user_post_videos` |
| 小红书 | 网关的 Xiaohongshu App 端点：`search_users`、`search_notes`、`get_user_info`、`get_user_posted_notes`、`get_note_comments` |
| 视频号 / 快手 / B站 / 微博 | 网关对应平台端点，先查 OpenAPI 再动手 |
| TikTok / YouTube / Instagram / X / Reddit | 网关对应端点；需要复杂检索语法时再用用户已有的平台专用工具 |
| 任何新平台 | 先查 OpenAPI 确认方法、参数、分页与单价，不靠猜 |

端点参数与已知坑位见 `references/api-endpoints.md`。

## 默认口径

用户已给足信息时直接执行；缺口影响费用或范围时再追问。

| 项目 | 默认值 |
| --- | --- |
| 账号作品范围 | 拉全量；先取 1 页验证字段 |
| 评论 | 每篇 1 页顶层评论；全量与楼中楼另估 |
| 媒体下载 | 只在有明确用途时下载 |
| 逐字稿 | 平台官方字幕优先，否则第三方 ASR |
| 交付 | 原始 JSON + 统一字段 CSV + 报告 |
| 付费动作 | 先预览、先样本、再确认批量 |

## 标准工作流

### 第一步：定义任务

先归类，避免一上来全抓：

- **单篇内容** → 拆一篇的结构与评论区；
- **账号批量** → 拉历史作品做定位与内容分析；
- **关键词 / 话题** → 搜索筛选候选选题；
- **对标资产包** → 多账号横向对比，产出可复用打法。

确认：平台、对象（账号/链接/关键词）、范围与样本量、需要的字段、交付物、输出目录。

### 第二步：查端点

按顺序找四类端点：**账号发现 → 作品列表 → 单条详情/评论 → 字幕或媒体地址**。记录每类端点的请求方式、单价、每页条数与限制。

### 第三步：算请求数与费用

```text
总请求数 = 账号资料 + 作品列表页数 + 单条详情 × 条数
          + 作品数 × 每条评论页数 + 楼中楼页数 + 字幕/下载等补充端点
```

离线预估（不发请求）：

```bash
python3 scripts/api_request.py \
  --path '/api/v1/<platform>/<endpoint>' \
  --estimate-requests 106 --unit-price 0.001 --dry-run
```

有 Key 时用官方价格接口核准：

```bash
python3 scripts/api_request.py \
  --official-price \
  --path '/api/v1/<platform>/<endpoint>' \
  --estimate-requests 106
```

多个不同单价的端点分别计算后相加；第三方转写费用单独列账，不混进网关费用。

### 第四步：小样本验证

先 dry-run 看脱敏后的请求预览：

```bash
python3 scripts/api_request.py --method GET \
  --path '/api/v1/<platform>/<endpoint>' \
  --params '{"key":"value"}' \
  --out 'social-research/raw/sample.json' --dry-run
```

确认后去掉 `--dry-run`，只跑 1–3 条。

### 第五步：按成本顺序采集

1. 账号资料（最便宜，优先）；
2. 作品列表（用 `collect_notes.py` 一次拉全量）；
3. 评论（确认有价值再加深）；
4. 媒体与字幕（按需）。

采集时把每次请求记进账本，方便事后核对：

```bash
python3 scripts/collect_notes.py \
  --platform xiaohongshu \
  --user-id '<user_id>' \
  --tag '<短标识>' \
  --out-dir social-research/raw \
  --ledger social-research/ledger.json \
  --resume
```

关键词选题筛选：

```bash
python3 scripts/search_pick.py \
  --platform xiaohongshu --keyword '早睡' \
  --pages 2 --tag zaoshui \
  --out-dir social-research/raw \
  --enrich-author 10 \
  --out-csv social-research/normalized/zaoshui_candidates.csv
```

### 第六步：归档与归一化

```bash
python3 scripts/normalize.py \
  --input social-research/raw/<tag>_merged.json \
  --out-csv social-research/normalized/<tag>_all_notes.csv \
  --out-json social-research/normalized/<tag>_all_notes.json \
  --stats social-research/normalized/<tag>_stats.md
```

推荐目录（原始文件永不覆盖）：

```text
social-research/
├── raw/            # 原始响应，逐页保存，不加工
├── normalized/     # 统一字段的 CSV / JSON / 数据摘要
├── media/          # 明确需要的封面、图片、视频
├── transcripts/    # 官方字幕或第三方 ASR 结果
├── evidence/       # 原链接、截图、引用证据
├── reports/        # 最终报告
└── ledger.json     # 请求账本（端点、时间、成功与否）
```

字段标准见 `references/output-schema.md`。

### 第七步：分析与交付

按四维框架（人设 / 人群 / 内容 / 商业）拆解，方法论与报告结构见 `references/analysis-framework.md`。

**最终报告必须输出为 HTML 文件**，不允许用 Markdown 充当最终报告：

- 以 `assets/report-template.html` 为骨架（奶油黄配色、四维结构），把本次数据与结论填入对应区块后，写到输出目录的 `reports/` 下；
- Markdown 只能作为附加摘要，不能替代 HTML；
- 仅当用户明确说「只要 Markdown / 不要 HTML」时才降级为 md。

交付清单：

- 数据摘要（脚本自动生成，先把事实摆出来）；
- 四维定位拆解；
- 内容主线与爆款结构；
- 评论区三类信号（需求 / 异议 / 转化）；
- 可借鉴点与风险点；
- 本次请求数、费用、缺失字段与待验证项。

## 常见误判（务必避免）

| 误判 | 正确做法 |
| --- | --- |
| 用中间文件做分析，漏掉爆款 | 一律回到原始分页文件或 merged 全量重建 |
| 把平台缺失字段（如播放量为 0）当真实表现 | 明确写「该字段不可用」，改用赞/评/藏/转 |
| 把推断的客单价写成事实 | 标注待验证 + 写明推断依据 |
| 粉丝数从搜索接口直接读 | 搜索接口的粉丝数常不准，需另取账号资料 |
| 同名账号直接开抓 | 先列候选、比对简介，请用户确认目标 |
| 日期短格式（如 `07-06`）直接当年份 | 标注日期置信度，不满一年内的不写年度结论 |

## 错误处理

| 现象 | 处理 |
| --- | --- |
| 没有 Key | 停止付费采集，给本机配置步骤；**不要**偷偷换成另一套手动采集 |
| 脚本报「未检测到 API Key」但已配置过 | 环境变量没被当前 shell 加载（macOS 非交互 shell 常见）。先确认当前会话能读到变量，或改用系统钥匙串，详见 `references/configuration.md` |
| 401 | Key 无效/过期或请求头格式错误 |
| 402 | 余额或额度不足，建议缩小范围或换低成本端点 |
| 403 | 该端点权限不足 |
| 404 | 端点路径不存在，以 OpenAPI 为准 |
| 429 | 触发限速，降并发、缩范围或延迟重试（脚本已内置退避重试） |
| 请求成功但无数据 | 核对对象、地区、权限、时间范围与分页参数 |
| 字段与文档不一致 | 保留原始响应，更新映射并记录日期，不改写原始数据 |
| 网络不可达 | 确认网络；本机代理不可用时绕过代理直连 |
| 成本超预估 | 立即暂停，重新出预览 |

## 交付验收清单

- [ ] 交付了 HTML 报告文件（基于 `assets/report-template.html` 骨架），md 只作附加摘要
- [ ] 平台、对象、范围、采集时间、数据来源写清楚
- [ ] 样本通过后才批量
- [ ] 请求数与费用有记录（ledger 可查）
- [ ] 原始响应未覆盖，结构化结果可回溯
- [ ] 评论深度与逐字稿来源写明
- [ ] 交付物不含密钥、Cookie 或临时下载凭据
- [ ] 每条推断都标注了置信度，客单价一类推断标了待验证
- [ ] 没有把计划中的自动化写成已经跑完

## 示例

**输入：** 用碗子调研拆解这个小红书博主，从人设、人群、内容、商业四个维度分析。

**动作：** 搜索定位账号（多同名时先列候选请用户确认）→ 取账号资料 → `collect_notes.py` 拉全量作品 → `normalize.py` 出统一表和摘要 → 抓 2–3 篇代表作的评论 → 按四维框架写报告，标注待验证项。

**输入：** 搜今天讲「早睡」的内容，找低粉但数据好的选题。

**动作：** `search_pick.py` 搜索并翻页 → 用 `--enrich-author` 补齐作者粉丝数 → 按效率分排序 → 人工复核标题与内容，输出候选选题表，并说明效率分是内部排序用的经验权重、不是平台指标。

**输入：** 这个技能免费吗？调研 20 个账号要多少钱？

**回答：** 技能本身免费开源；数据采集走你自己的付费网关，费用由你直接支付。要先按每个账号的作品数、评论深度和具体端点拆请求数，再用官方价格接口核准，只给带来源的估算，不承诺固定金额。
